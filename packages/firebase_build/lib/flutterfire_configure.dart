/// Driving the `flutterfire` cli: generating the firebase options of a
/// flutter app (`lib/firebase_options.dart`, or one file per flavor) from
/// its firebase project, with [FirebaseFlutterProjectBuilder].
library;

export 'src/flutterfire_configure.dart'
    show FirebaseFlutterProjectBuilder, FirebaseFlutterProjectOptions;
